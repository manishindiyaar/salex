# Story 3.2: Centralized API Client Implementation
Status: Completed

## Epic
Frontend-Backend Integration - Communication Foundation

## Story
**As a** developer  
**I want** a centralized API client with authentication and error handling  
**So that** all frontend services can communicate securely with the backend

## Acceptance Criteria
(Updated 2025-08-05: All acceptance criteria implemented; API client validated end-to-end)

### API Client Architecture
- [x] Create centralized `APIClient` class with Axios
- [x] Configure base URL for development and production environments
- [x] Implement request/response interceptors
- [x] Add automatic Firebase token injection to requests
- [x] Handle token refresh automatically on 401 responses
- [x] Implement standardized error handling

### HTTP Methods Implementation
- [x] Implement `get<T>(endpoint, config)` method
- [x] Implement `post<T>(endpoint, data, config)` method
- [x] Implement `put<T>(endpoint, data, config)` method
- [x] Implement `delete<T>(endpoint, config)` method
- [x] All methods return `ApiResponse<T>` format
- [x] Type safety with shared-types package

### Error Handling System
- [x] Global error interceptor for common HTTP errors
- [x] Network error handling and retry logic
- [x] 401 authentication error handling
- [x] User-friendly error message formatting
- [x] Development vs production error logging

### Environment Configuration
- [x] Configure API base URL for development (localhost:3000)
- [x] Add production URL configuration
- [x] Environment-based timeout settings
- [x] Debug logging in development mode

## Technical Requirements

### API Client Structure
```typescript
class APIClient {
  private client: AxiosInstance;
  private baseURL = __DEV__ ? 'http://localhost:3000/api/v1' : 'https://api.salex.app/api/v1';

  constructor() {
    this.client = axios.create({
      baseURL: this.baseURL,
      timeout: 10000,
      headers: {
        'Content-Type': 'application/json',
      },
    });
    this.setupInterceptors();
  }
}
```

### Request Interceptor
- Automatically inject Firebase tokens
- Add request ID for tracking
- Log requests in development

### Response Interceptor
- Handle 401 token expiration
- Format error responses consistently
- Log responses in development

## Definition of Done
(Completed)
- [x] APIClient class implemented and tested
- [x] All HTTP methods work with proper typing
- [x] Authentication tokens automatically included
- [x] Error handling works for common scenarios
- [x] Environment configuration properly set up
- [x] Documentation for other developers
- [x] Integration tests pass

## Dependencies
- Firebase Authentication Integration (Story 3.1)
- Axios HTTP client library
- AsyncStorage for token management

## Estimated Effort
3 story points (1-2 days)

## Test Scenarios
1. **Authenticated Requests**: Token automatically added to headers
2. **Token Expiration**: 401 response triggers re-authentication
3. **Network Errors**: Proper error handling and user messaging
4. **Type Safety**: Requests and responses properly typed
5. **Environment Switching**: Correct API URL for dev/prod

## Implementation Notes
- Create singleton instance for consistent usage
- Use existing shared-types for request/response typing
- Follow React Native best practices for HTTP clients
- Consider implementing request queuing for offline scenarios

## Files to Create/Modify
- `apps/MerchantApp/src/services/apiClient.ts` - New centralized client
- `apps/MerchantApp/src/services/types.ts` - HTTP client types if needed
- Update existing service files to use new client

## Status
Completed on 2025-08-05

---
*Sprint 1 - Week 1 - Priority: Critical*
