import { PrismaService } from '../../../core/prisma.service';
import { BookingService } from '../../booking/booking.service';
import { BusinessService } from '../../business/business.service';
import { ServiceService } from '../../service/service.service';
import { TimeSlotsService } from '../../timeslots/timeslots.service';
import { WhatsAppOutgoingMessage } from 'shared-types';
export interface BookingFlowState {
    step: 'GREETING' | 'CONNECTING' | 'SERVICES' | 'TIMESLOTS' | 'CONFIRMATION' | 'BOOKING_COMPLETE' | 'MY_BOOKINGS' | 'CANCEL_BOOKING';
    businessId?: string;
    selectedServiceId?: string;
    selectedTimeSlot?: string;
    selectedDate?: string;
    bookingId?: string;
    customerName?: string;
    previousStep?: string;
}
export declare class BookingFlowService {
    private readonly prisma;
    private readonly bookingService;
    private readonly businessService;
    private readonly serviceService;
    private readonly timeSlotsService;
    private readonly logger;
    constructor(prisma: PrismaService, bookingService: BookingService, businessService: BusinessService, serviceService: ServiceService, timeSlotsService: TimeSlotsService);
    processBookingMessage(customerPhone: string, messageText: string, interactiveData?: any, currentState?: BookingFlowState): Promise<WhatsAppOutgoingMessage[]>;
    private handleTextMessage;
    private handleInteractiveResponse;
    private handleButtonClick;
    private handleListSelection;
    private connectToBusiness;
    private showServicesMenu;
    private selectService;
    private selectTimeSlot;
    private confirmBooking;
    private showMyBookings;
    private cancelBooking;
    private goToPreviousStep;
    private createGreetingMessage;
    private createRoutingCodePrompt;
    private createConnectionSuccessMessage;
    private createServicesListMessage;
    private createTimeSlotsMessage;
    private createBookingConfirmationMessage;
    private createBookingSuccessMessage;
    private createMyBookingsMessage;
    private createNoBookingsMessage;
    private extractRoutingCode;
    private formatDate;
    private formatTime;
    private saveSessionState;
    private getSessionState;
    private createBusinessNotFoundMessage;
    private createInvalidCodeMessage;
    private createErrorMessage;
    private createUnknownCommandMessage;
    private createBookingErrorMessage;
    private createBookingCancelledMessage;
    private showBookingDetails;
    private showCancelConfirmation;
}
